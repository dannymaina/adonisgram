@layout('layouts/default')

@section('content')
  <div>
    <h1>Sign In</h1>
    <p>{{ flashMessages.get('success') || '' }}</p>
    <form action="{{route('signin_user')}}" method="POST">
      {{ csrfField() }}
      <div>
        <label for="email">Email</label>
        <input type="text" name="email" id="email" value="{{ flashMessages.get('email') || '' }}">
        <p>{{ flashMessages.get('errors.email') || '' }}</p>
      </div>
      <div>
        <label for="password">Password</label>
        <input type="password" name="password" id="password">
        <p>{{ flashMessages.get('errors.password') || '' }}</p>
      </div>
        <button type="submit">Sign in</button>
      </div>
    </form>
  </div>
@endsection
